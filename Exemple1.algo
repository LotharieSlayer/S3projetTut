Algorithme Exemple
Constante :
 TAUX_CORR <-- 0.9
 REPONSE <-- vrai
Variable :
 tab : tableau[8][9][10] d'entiers
 long,larg : entier
 aire : entier
DEBUT
long <-- 3
larg <-- 8
aire <-- long + larg × 4
ecrire ( aire )
aire <-- (long + larg) × 4
ecrire ( aire © " Patates")
tab[0][0][0] <-- 5 + 8
ecrire ( tab[0][0][0] × 2 )
si 2 = 2 alors
    ecrire ("Michel c'est le brésil")
fsi